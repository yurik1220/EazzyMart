{
  "name": "grocery-admin-system",
  "version": "1.0.0",
  "description": "Grocery admin system with tax report generator",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "generate-tax-report": "node scripts/taxReport.js",
    "db:backup": "node database/backup-sqlite.js",
    "db:migrate": "node database/migrate-to-postgres.js",
    "db:compare": "node database/compare-databases.js",
    "db:test-pg": "node -e \"const {Pool}=require('pg');const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});pool.query('SELECT NOW()').then(r=>{console.log('✅ Connected:',r.rows[0]);process.exit(0)}).catch(e=>{console.error('❌ Error:',e.message);process.exit(1)})\"",
    "db:test-setup": "node database/test-setup.js",
    "db:cleanup-sqlite": "echo '📚 See REMOVE_SQLITE_GUIDE.md for safe SQLite removal instructions'"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "body-parser": "^1.20.3",
    "broadcast-channel": "^7.1.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "express-session": "^1.18.2",
    "multer": "^1.4.5-lts.2",
    "nodemailer": "^7.0.10",
    "pg": "^8.16.3",
    "pm2": "^6.0.13",
    "resend": "^6.4.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
